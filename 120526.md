Fila

```

#include <stdio.h>

#define MAX_FILA 5

typedef int Elemento;
typedef struct{
Elemento v[MAX_FILA];
int inicio;
int fim;
} Fila;

void file_init (Fila *f) {f-> inicio = f->fim = -1;}

int enfileirar(Fila *f, Elemento e){
if((f->inicio == 0 && f->fim == MAX_FILA -1) || (f->fim + 1 == f->inicio)){
return 0;
}


if(f->) inicio == -1){
f->inicio = f->fim = 0;
}else if(f->fim==MAX_FILA -1){
f->fim = 0;
}else{
f->fim++;
}
f->v[f->fim] = e;
return1;
}
```
