#Modularização#
dia 28/4/26

Consiste em deompor um programa em uma serie de subprogramas individuais

Funções

  int soma(int a, int y){
  int s;
  s = x + y;
  return (s);
  }

  vantagem de ser antes do main - programa ja reserva a funcao 


  float = qando for falar de pessoa, altura, etc - impreciso pra operacoes cientificas (nao usar pra var monetario)
  double = operacoes matematicas moneratias, bancos, etc

  void retorna conteudo indeterminado (nao tem retorno > void)

#Procedimento#

```
#include <stdio.h>
void linha(void){
printf("------------- \n");
}

int main(void){
printf("Antes da linha");

```

Exercicio:

1 - Programa para verificar se um nmero é primo em C e em C++, aplicando funcao(cm retrno) e procedimento (void) para verificar se um numero é primo

```
#include <stdbool.h>
#include <stdio.h>

bool eh_primo(int n){
if(n < 2)
return false;
for(int i = 2; i * i <= n;i++){
if (n % i == 0){
return false;
}
return true;
}
}

void mostra_resultado(int n){
if(eh_primo(n)){

printf("%d eh primo \n", n);
}else{

printf("%d nao eh primo. \n", n);
}
}

int main(void){
int numero;
printf("Digite um numero:");
scanf("%d", &numero);
mostra_resultado(numero);
return 0;
}
```
PUTCHAR
PUTS
GETCHAR
GETS
Dao o flush automatico

PUTS - Assim que passa, ele limpa a sujeira como um garbage collector)

Nao decorar sigla, e sim o significado

fopen - abre um arquivo 
r
w
a
r+
w+

hoje é tdo relaxado com memoria, antigamente era tudo lapidado
(em jogos o uso de memoria é prioridade)

Ex.:

#include <stdio.h>
#include <stdlib.h>

int main(void){
FILE *arquivo;
char nome[];
float nota;

arquiv = fopen();
if(arquivo == null){
perror
return 1;
}

printf("Digite o nome o nome do aluno");
fgets(nome, sizeof nome, stdin); // 


fclose (sempre usar pra fechar o arqivo - questao de segurança)
Pesquisar como gerar um LOG ()



```
#include <stdio.h>
#include <stdlib.h>

```
Exercicio:

2 - Boletim de Alunos (C)
 > 1. Crie uma struct Aluno com: nome, curso, vetor notas[3] (floats) e media (float);
 > 2. Leia N(1..5) alunis. Para cada aluno, leia nome e curso (com espaços) e 3 notas.
 > 3. calcule a media de cada aluno em uma funcao que receba o ponteiro para Aluno (passagem por referencia)
 > 4. Escreva todos os alunos em um arquivo CSV chamado boletim.csv no formato: nome; curso; nota1; nota2; nota3; media
 > 5. Reabra o arquivo em modo leitura e imprima seu conteudo na tela
 > 6. Implemente uma funcao para buscar por nome (com strcmp) e imprimir o aluno encontrado.
 > 7. Usando aritmetica de ponteiros, encontre o aluno com maior media e mostre seu resumo.
 > 8. Use boas praticas de entrada (fgets para strings, limpeza do buffer apos scanf), verifique erros em fopen, e feche os arquivos

FGETC FPUTC UNGETC

ISUPPER ISALPHA TOLOWER

STRLEN STRCPY STRCMP STRCAT

(Quando for em entrevista e nao soube algo, pergunte)
Treinar logica

Passagem de parametros

```
#include <stdio.h:

void por_valor (int x) {
x = x + 10;
}
void por_referencia (int *x) {
*x = *x + 10;
}

int main (void){
in a =5, b= 5;

por_valor(a);
por_referencia(&b);

printf("a (por vaor) = %d \n", a);
printf("b (por referencia) = %d \n", b);

return 0;
}
```
