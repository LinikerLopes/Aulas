#Listas, Pilhas e Filas#

Listas - estruturas de  dados nao primitivas, usadas para reunir um conjunto de elementos que guardam  relacao entre si

O de n 

Brute Force

```
#include <stdio.h>

typedef  struct{
int chave;
}Item;

int buscar (const Item lista[], int n, int chave){
for (int i = 0; i < n; i++){
if (lista[i].chave == chave){
return i;
}
}
return -1; //quando nao encontrado
}

int main (void){
Item v[5] = {{5}, {8}, {13}, {21}, {34}};
int n = 5;

printf("buscar 21 -> %d \n", buscar(v, n, 21));
printf("buscar 99 -> %d \n", buscar(v, n, 99));
return 0;
}

```

Conceitos e operacoes em listas

Inserção

int inserir (novo elemento) se busca (novo elemento.chave) == n + 1
lista [ n+1] == novo elemento
n = n +1
retornar 1
senao retornar -1

Quando vc quer alocar um novo valor, elemento, vc procura um espaço 
Quando nao encontrar geralmente coloca no inicio ou no fim, ou reorganiza ela

oque ela para uma árvore

remoção

se n > 0
int i = busca (chave)
se i < n + 1
para a =  ate a < n
lista [i] = lista [ i+1]
n = n - 1
senao retornar -1
senao erro lsita vazia

Criar struct para guardar 
```
#include <stdio.h>
#include <stdlib.h>

typedef struct {
int chave;
} Item;

typedef struct{
Item *v;
size_t n, cap;
} Lista;

static void lista_init (Lista *L){
L->v = NULL;
L->n = 0;
L->cap = 0;
}

static void lista_free (Lista *L){
free(L->v);
L->v = NULL;
L->n = L->cap = 0;
}

//insercao

int inserir (Lista *L, Item x){
if ( L->n == L->cap){
size_t nova = L-> cap ? L-> cap * 2 : 4;
Item *p = (Item *) realloc (L ->v, nova * sizeof *p);
if(!p)
return -2;
L -> v = p;
L-> cap = nova;
}
L -> v [L->n++] = x;
return 1;
}

int main (void){
Lista L;
lista_init(&L);
inserir(&L, (Item){.chave = 10});
inserir(&L, (Item){.chave = 20});
inserir(&L, (Item){.chave = 30});

for (size_t i = 0; i < L.n; ++i){
printf("%d ", L.v[i].chave);
puts("");

lista_free(&L);
return 0;
}
}

```

Alocacao dinamica  (faca de dois gumes)
[..]
int *vetor;
vetor = (int *) malloc (tamanho_vetor * sizeof (int )); 
(tem de dar um limite)

Alocacao encadeada é uma forma de se contornar esse problema, redunzino a sobrecarga com o gerenciamento de memoria

struc No {
int chave;
Elemento elemento
}

A ideia é fazer a linjagem om struct

Lista encadeada


